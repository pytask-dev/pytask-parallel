name: pytask-parallel

channels:
  - conda-forge
  - nodefaults

dependencies:
  - python
  - pip
  - setuptools_scm
  - toml

  # Package dependencies
  - pytask>=0.4.5
  - cloudpickle
  - loky
  - optree

  # Misc
  - tox
  - ipywidgets
  - nbmake
  - pytest-cov

  - pip:
    - -e .

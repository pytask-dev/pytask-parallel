name: pytask-parallel

channels:
  - conda-forge
  - nodefaults

dependencies:
  - python
  - pip
  - setuptools_scm
  - toml

  # Package dependencies
  - conda-forge/label/pytask_rc::pytask=0.4.0rc2
  - cloudpickle
  - loky
  - optree

  # Misc
  - black
  - pre-commit
  - pytest-cov

  - pip:
    - -e .

[tox]
envlist = pytest

[testenv]
usedevelop = true

[testenv:pytest]
conda_channels =
    conda-forge
    nodefaults
conda_deps =
    cloudpickle
    loky
    conda-forge/label/pytask_rc::pytask =0.4.0rc2
    pytest
    pytest-cov
    pytest-xdist
commands =
    pip install --no-deps -e .
    pytest {posargs}
